# 中航信标准治理系统总体设计文档

## 1. 文档概述

### 1.1 项目名称

**中航信标准治理系统**

系统原型工程名为 `semAlign`，当前代码分为：

- 前端工程：`semAlign/semAlign`
- 后端工程：`semAlign/semAlign_backend`

本文档基于现有 `semAlign` 前后端实现，整理系统的标准检索、标准治理、标准语义对齐、人工审核与发布等总体设计思路，并形成可作为项目总体设计说明的文档。

### 1.2 建设目标

中航信标准治理系统面向企业内部标准文档的全生命周期管理，核心目标包括：

1. 建立标准文档统一入口，实现标准文档上传、解析、入库、检索、查看与下载。
2. 建立标准语义检索能力，通过元数据检索、全文检索、向量检索提升标准查找效率。
3. 建立标准治理工作台，展示标准总量、月度新增、待处理对齐任务、生命周期分布、分类分布和标准动态。
4. 建立标准对齐与冲突识别能力，对两个标准之间的条款进行语义匹配、冲突识别、优先级排序和解决建议生成。
5. 建立人工决策与审核发布流程，使管理员能够对对齐结果进行审核、发布，普通用户查看审核后的标准对齐数据。
6. 建立用户与权限体系，区分普通用户和管理员的功能范围。
7. 接入大模型网关，支持标准对齐助手、RAG 问答、对齐结果解释和后续 AI 复核能力。

### 1.3 系统定位

系统定位为企业级标准治理平台，主要解决以下问题：

- 标准文档分散，难以统一查找和复用。
- 标准文本内容长，人工阅读成本高。
- 不同标准之间存在条款重叠、冲突和缺失，人工比对效率低。
- 标准生命周期、导入记录、对齐任务和审核状态缺少统一管理。
- 普通用户和管理员的职责边界不清晰。

系统通过“文档入库 + 语义检索 + 标准对齐 + 人工治理 + 审核发布”的方式，形成标准治理闭环。

---

## 2. 总体架构

### 2.1 架构概览

系统采用前后端分离架构：

```text
用户浏览器
   │
   │ HTTP / JSON / 文件上传
   ▼
React + Vite 前端
   │
   │ Axios API 调用
   ▼
FastAPI 后端服务
   │
   ├── SQLite / PostgreSQL：结构化业务数据
   ├── 文件存储：原始标准文档、解析文本、图片
   ├── Chroma 向量库：标准内容向量索引
   ├── BM25 索引：关键词检索增强
   └── OpenAI-compatible LLM 网关：标准问答与智能解释
```

当前开发环境默认使用：

- 前端端口：`5173`
- 后端端口：`8000`
- 数据库：`SQLite ./data/semalign.db`
- 向量库：`./data/chroma_db`
- 本地 embedding 模型：`./models/gte-multilingual-base`

### 2.2 前端技术栈

前端位于 `semAlign/semAlign`，采用：

- React 19
- TypeScript
- Vite
- React Router
- Axios
- Zustand
- Tailwind CSS
- Recharts
- lucide-react

主要目录：

```text
src/
├── api/                    # Axios、接口模块、endpoint 定义
├── components/             # 通用布局、按钮、卡片、分页、弹窗、Toast
├── features/
│   ├── auth/               # 登录、忘记密码
│   ├── workbench/          # 工作台
│   ├── standards/          # 标准数据库、详情、下载
│   ├── search/             # 智能检索
│   ├── import/             # 标准导入、导入历史
│   ├── alignment/          # 标准对齐、任务列表、结果页
│   └── user/               # 用户资料、修改密码、用户管理
├── store/                  # 登录状态和全局状态
├── router.tsx              # 前端路由
└── types/                  # 前端类型定义
```

### 2.3 后端技术栈

后端位于 `semAlign/semAlign_backend`，采用：

- FastAPI
- SQLAlchemy
- Pydantic / pydantic-settings
- JWT 认证
- SQLite / PostgreSQL
- ChromaDB
- BM25
- Sentence Transformer / 本地 embedding 模型
- OpenAI-compatible SDK

主要目录：

```text
semAlign_backend/
├── main.py                 # FastAPI 应用入口、路由注册
├── core/                   # 配置、数据库、认证、安全、依赖注入
├── models/                 # SQLAlchemy 模型
├── schemas/                # Pydantic 请求响应模型
├── routers/                # API 路由
├── services/               # 对齐、冲突检测、规则、导出等业务服务
├── utils/                  # 文档解析、PDF、校验、向量工具
├── scripts/                # 初始化数据库、初始化用户等脚本
└── data/                   # 开发环境数据文件
```

### 2.4 核心后端路由

```text
/api/auth/*                 用户登录、忘记密码、当前用户
/api/user/*                 用户资料、修改密码、管理员用户管理、权限
/api/workbench/*            工作台统计
/api/standards/*            标准 CRUD、筛选、详情内容、下载
/api/import/*               标准上传、导入、导入历史
/api/search/*               智能检索、搜索建议
/api/alignment/*            对齐任务、任务列表、重试、删除、人工决策、审核发布、AI 助手
/api/comparison/*           对齐结果任务信息、统计、冲突分页
/api/export/*               报告导出
/api/rules/*                规则管理
```

---

## 3. 业务角色与权限设计

### 3.1 角色定义

系统当前设计两个基础角色：

| 角色 | 说明 |
|---|---|
| 普通用户 `user` | 主要进行智能检索、查看标准、查看管理员发布后的标准对齐结果 |
| 管理员 `admin` | 负责标准上传、标准入库、标准维护、对齐任务管理、审核发布、用户管理 |

### 3.2 权限边界

| 功能 | 普通用户 | 管理员 |
|---|---:|---:|
| 登录系统 | 是 | 是 |
| 智能检索 | 是 | 是 |
| 查看标准数据库 | 是 | 是 |
| 查看标准详情 / 下载标准 | 是 | 是 |
| 上传标准文档 | 否 | 是 |
| 批量导入标准 | 否 | 是 |
| 创建标准对齐任务 | 视业务策略，可限制 | 是 |
| 管理对齐任务 | 否 | 是 |
| 查看已发布对齐结果 | 是 | 是 |
| 审核 / 发布对齐结果 | 否 | 是 |
| 用户管理 | 否 | 是 |
| 修改个人资料 / 密码 | 是 | 是 |

### 3.3 权限接口

后端提供：

```text
GET /api/user/permissions
```

用于返回当前用户角色和前端可用功能开关，例如：

```json
{
  "role": "admin",
  "permissions": {
    "search": true,
    "view_reviewed_alignment": true,
    "import_standards": true,
    "manage_alignment_tasks": true,
    "manage_users": true
  }
}
```

---

## 4. 标准数据库与标准治理思路

### 4.1 标准数据对象

标准数据库的核心对象是 `Standard`，用于保存标准文档的元数据：

- 标准编号 `standard_no`
- 标准名称 `name`
- 版本 `version`
- 状态 `status`
- 分类 `category`
- 部门 `department`
- 描述 `description`
- 来源文件 `source_file`
- 创建时间 / 更新时间

标准治理不是单纯存文件，而是将标准文档拆为多个层次：

```text
原始文件层：PDF / Excel
元数据层：标准号、名称、版本、部门、状态、分类
文本层：解析出的正文文本
索引层：全文索引、BM25 索引、向量索引
对齐层：条款切分、匹配关系、冲突结果、人工决策
治理层：审核状态、发布状态、用户权限、动态统计
```

### 4.2 标准状态治理

标准状态用于表达标准生命周期，当前状态可包括：

- 新增
- 草稿
- 审核中
- 有效
- 待废止
- 已废止

工作台会将状态映射到生命周期阶段：

```text
新增 / 草稿  → 草案
审核中       → 评审中
有效         → 已生效
待废止       → 待废止
已废止       → 已废止
```

### 4.3 标准筛选与查看

标准数据库页面支持：

- 关键词搜索
- 状态筛选
- 部门筛选
- 分类筛选
- 分页展示
- 标准详情查看
- 原文件下载
- 解析文本下载

对应后端接口：

```text
GET /api/standards
GET /api/standards/filter-options
GET /api/standards/{standard_id}
GET /api/standards/{standard_id}/content
GET /api/standards/{standard_id}/download?kind=original
GET /api/standards/{standard_id}/download?kind=text
```

### 4.4 标准导入治理

标准导入流程包括：

1. 管理员上传文件。
2. 后端校验文件类型和大小。
3. 文件保存到上传目录。
4. 文档解析，抽取正文和图片。
5. 保存解析文本。
6. 生成导入预览和校验结果。
7. 管理员确认导入标准元数据。
8. 标准数据写入数据库。
9. 后台写入向量库。
10. 记录导入历史。

支持文件类型：

- `.pdf`
- `.xlsx`
- `.xls`

对应后端接口：

```text
POST /api/import/upload
POST /api/import
POST /api/import/confirm
GET  /api/import/history
```

---

## 5. 标准检索设计思路

### 5.1 检索目标

标准检索的目标是帮助用户快速定位相关标准、条款、来源文档和上下文片段。

检索能力应同时覆盖：

- 标准编号检索
- 标准名称检索
- 描述 / 分类 / 部门检索
- 原始文件名检索
- 解析正文检索
- 向量语义检索
- 搜索建议
- 后续 RAG 问答引用

### 5.2 检索分层设计

系统检索分为三层：

#### 5.2.1 元数据检索

直接查询关系数据库中的标准元数据，适合：

- 标准号精确匹配
- 标准名称模糊匹配
- 分类 / 部门 / 状态筛选
- 描述字段搜索

接口：

```text
GET /api/search?keyword=xxx
GET /api/standards?search=xxx&status=xxx&department=xxx
```

#### 5.2.2 文本关键词检索

通过解析文本、BM25 索引或数据库辅助字段进行关键词召回，适合：

- 用户输入专业词汇
- 标准条款中的术语检索
- 文档正文中出现但元数据未出现的内容

#### 5.2.3 向量语义检索

通过本地 embedding 模型将标准正文切分为 chunk 并写入 Chroma 向量库。

适合：

- 用户描述性查询
- 同义表达检索
- 非精确关键词检索
- 标准助手 RAG 上下文召回

配置项包括：

```text
VECTOR_STORE_ENABLED=true
CHROMA_DB_DIR=./data/chroma_db
EMBEDDING_MODEL_DIR=./models/gte-multilingual-base
VECTOR_CHUNK_SIZE=400
VECTOR_CHUNK_OVERLAP=50
BM25_WEIGHT=0.3
SEMANTIC_WEIGHT=0.7
```

### 5.3 混合检索策略

推荐检索策略：

```text
用户输入 query
   │
   ├── 元数据检索：标准号、名称、描述、部门、分类
   ├── BM25 检索：关键词相关 chunk
   └── 向量检索：语义相关 chunk
          │
          ▼
结果融合与去重
          │
          ▼
返回标准列表 + 命中摘要 + 来源文件 + 相关分数
```

结果排序建议综合：

- 标准号 / 名称命中优先
- 语义相似度
- BM25 分数
- 最近更新时间
- 标准状态，有效标准优先

### 5.4 检索结果结构建议

建议统一返回：

```json
{
  "id": 1,
  "standard_no": "GB/T xxx",
  "name": "标准名称",
  "version": "2024",
  "status": "有效",
  "category": "分类",
  "department": "部门",
  "source_file": "xxx.pdf",
  "match_excerpt": "命中的正文摘要...",
  "relevance_score": 0.86,
  "references": [
    {
      "section": "5.2.1",
      "page": 12,
      "excerpt": "..."
    }
  ]
}
```

---

## 6. 标准语义对齐设计思路

### 6.1 对齐目标

标准对齐的目标是识别两个标准之间的：

- 相同 / 相近条款
- 内容差异
- 潜在冲突
- 缺失条款
- 新增条款
- 优先级处理建议
- 人工决策记录

### 6.2 对齐任务模型

后端使用 `AlignmentTask` 管理对齐任务：

- 创建人 `user_id`
- 输入文本 `input_text`
- 任务状态 `status`
- 任务选项 `options`
- 结果 JSON `result_json`
- 完成时间 `completed_at`
- 审核状态 `review_status`
- 审核意见 `review_notes`
- 审核人 `reviewed_by`
- 审核时间 `reviewed_at`
- 发布时间 `published_at`

任务状态：

```text
pending → processing → completed / failed
```

审核状态：

```text
draft → submitted → approved → published
               └── rejected
```

### 6.3 对齐执行流程

当前系统采用后台任务执行对齐，避免接口长时间阻塞。

```text
前端选择标准 A、标准 B
        │
        ▼
POST /api/alignment/tasks
        │
        ▼
创建 AlignmentTask，状态 pending
        │
        ▼
后台任务执行
        │
        ├── 加载标准 A 正文
        ├── 加载标准 B 正文
        ├── 条款切分
        ├── 条款语义匹配
        ├── 冲突识别
        ├── 优先级排序
        ├── 生成解决建议
        ├── 输出未匹配条款
        └── 写回 result_json
        │
        ▼
任务状态 completed / failed
        │
        ▼
前端轮询任务状态并打开结果页
```

### 6.4 标准正文加载策略

对齐时优先加载有效正文，来源包括：

1. 向量库 chunk。
2. 已保存解析文本。
3. Chroma SQLite metadata。
4. 标准元数据兜底。

如果无法加载有效正文，任务会失败并返回明确错误。

### 6.5 条款切分设计

条款切分器基于标准常见编号和文本标点进行切分：

- 优先识别 `1`、`1.1`、`1.1.1` 等条款编号。
- 无法识别时按换行和句末标点切分。
- 过滤过短文本。
- 对过长条款进行截断。
- 对重复条款进行去重。
- 尝试保留章节号、页码和字符位置。

条款对象建议结构：

```json
{
  "idx": 1,
  "section": "5.2.1",
  "page": 12,
  "start_char": 3200,
  "text": "条款正文..."
}
```

### 6.6 条款匹配策略

匹配策略包括：

1. 优先使用 `ClauseAligner`，基于语义相似度匹配。
2. 如果模型不可用，则使用 fallback 匹配：
   - `SequenceMatcher` 文本相似度
   - token Jaccard 相似度
   - 加权计算综合分

输出匹配对：

```json
{
  "a_idx": 1,
  "b_idx": 3,
  "score": 0.82,
  "a_section": "5.2.1",
  "b_section": "6.1",
  "a_text": "...",
  "b_text": "...",
  "match_type": "semantic"
}
```

### 6.7 冲突识别策略

当前冲突识别以语义匹配结果和规则分析为基础，输出：

- 冲突标题
- 严重程度
- 相似度分数
- 优先级分数
- 优先级排名
- 位置
- 标准 A 内容
- 标准 B 内容
- 推荐处理意见
- 人工决策

后续建议扩展冲突类型：

- 术语定义冲突
- 数值阈值冲突
- 单位冲突
- 强制性要求冲突
- 适用范围冲突
- 版本冲突
- 引用标准冲突
- 新增 / 缺失条款

### 6.8 对齐结果结构

对齐结果保存在 `result_json`，建议结构：

```json
{
  "task_id": 1,
  "standard_group1": "标准 A",
  "standard_group2": "标准 B",
  "comparison_time": "2026-04-30 16:00:00",
  "alignment_mode": "semantic",
  "priority_rules": "...",
  "stats": {
    "conflict_rate": 20,
    "match_rate": 70,
    "pending_rate": 10,
    "conflict_count": 5,
    "match_count": 18,
    "pending_count": 2,
    "unmatched_group1_count": 3,
    "unmatched_group2_count": 4
  },
  "conflicts": [
    {
      "id": "conflict-1",
      "title": "条款差异",
      "severity": "medium",
      "location": {
        "standard1_clause_index": 1,
        "standard2_clause_index": 2,
        "standard1_section": "5.2.1",
        "standard2_section": "6.1",
        "standard1_page": 12,
        "standard2_page": 15,
        "standard1_excerpt": "...",
        "standard2_excerpt": "..."
      },
      "standard1": {
        "name": "标准 A",
        "content": "..."
      },
      "standard2": {
        "name": "标准 B",
        "content": "..."
      },
      "priority_recommendation": "建议人工确认",
      "manual_decision": null
    }
  ],
  "unmatched": {
    "standard1": [],
    "standard2": []
  }
}
```

---

## 7. 对齐结果治理与人工决策

### 7.1 对齐结果页面

前端结果页包括：

- 任务基本信息
- 标准组 1 / 标准组 2
- 对齐模式
- 优先级规则
- 比对时间
- 任务状态
- 冲突率 / 匹配率 / 待确认
- 冲突列表
- 分页
- 人工决策操作

对应后端接口：

```text
GET /api/comparison/task/{task_id}
GET /api/comparison/stats/{task_id}
GET /api/comparison/conflicts/{task_id}?page=1&size=20
```

### 7.2 人工决策

每条冲突支持：

- 接受建议
- 拒绝建议
- 手动修改
- 追加提示词 / 人工修改说明

接口：

```text
POST /api/alignment/tasks/{task_id}/manual-mapping
```

请求示例：

```json
{
  "conflict_id": "conflict-1",
  "decision": "modify",
  "notes": "请优先采用民航强制标准，并说明原因"
}
```

保存后写回对应冲突项的 `manual_decision`。

### 7.3 审核发布流程

管理员对标准对齐结果进行审核和发布：

```text
对齐完成 completed
        │
        ▼
草稿 draft
        │ 创建者提交
        ▼
待审核 submitted
        │ 管理员审核
        ├── 驳回 rejected
        └── 通过 approved
                 │ 管理员发布
                 ▼
              published
```

接口：

```text
POST /api/alignment/tasks/{task_id}/review
GET  /api/alignment/published
```

普通用户查看管理员发布后的对齐数据，管理员负责上传标准和管理对齐任务。

---

## 8. 工作台设计

### 8.1 工作台指标

工作台用于展示标准治理总体态势。

关键指标：

| 指标 | 含义 |
|---|---|
| 标准文档总数 | 当前标准数据库中标准文档记录数 |
| 本月新增文档 | 本月创建的标准文档记录数 |
| 待处理对齐任务 | 状态为 `pending` 或 `processing` 的对齐任务数量 |

### 8.2 标准动态

标准动态只展示标准文档相关动态，不展示问答对入库、术语冲突库导入等其他业务数据。

动态时间建议区分：

- 创建于：标准首次导入或创建时间
- 更新于：标准元数据后续更新时间

### 8.3 图表设计

工作台包括：

- 标准分类分布
- 生命周期分布
- 生命周期阶段分布
- 标准流程效率指标
- 最近标准动态

标准流程效率指标横轴统一使用：

```text
YYYY年M月
```

避免同时出现“年月格式”和“月格式”造成理解歧义。

### 8.4 说明性设计

由于部分指标当前为代理数据或阶段性实现，前端页面应明确标注：

- 指标统计口径
- 趋势计算方式
- 效率指标是否为真实 SLA
- 动态日期含义

---

## 9. 标准导入与导入历史 Pipeline

### 9.1 标准上传 Pipeline

```text
管理员选择文件
        │
        ▼
前端 multipart/form-data 上传
        │
        ▼
POST /api/import/upload
        │
        ├── 文件类型校验
        ├── 文件大小校验
        ├── 文件保存到 data/uploads
        ├── PDF / Excel 解析
        ├── 文本保存到 data/texts
        ├── 图片保存到 data/images
        ├── 解析结果校验
        ├── 生成导入预览
        └── 后台写入向量库
        │
        ▼
返回上传结果和待导入标准数据
```

### 9.2 标准确认导入 Pipeline

```text
前端展示导入预览
        │
        ▼
管理员确认导入
        │
        ▼
POST /api/import/confirm 或 POST /api/import
        │
        ├── 标准号去重
        ├── 已存在则更新
        ├── 不存在则新增
        ├── 写入 Standard 表
        ├── 同步标准向量索引
        └── 写入 ImportHistory
        │
        ▼
标准进入数据库
```

### 9.3 导入历史

导入历史页面用于查看：

- 上传文件名
- 上传人
- 导入时间
- 成功数量
- 失败数量
- 状态
- 错误信息

接口：

```text
GET /api/import/history
```

---

## 10. AI 标准对齐助手设计

### 10.1 功能定位

AI 标准对齐助手用于：

- 回答标准相关问题
- 解释两个标准的差异
- 给出冲突处理建议
- 根据所选标准上下文进行问答
- 后续可用于对齐结果复核

### 10.2 调用链路

```text
前端标准对齐页面输入问题
        │
        ▼
POST /api/alignment/chat
        │
        ├── 判断是否选择两个标准
        │      ├── 是：加载两个标准正文作为上下文
        │      └── 否：从标准库和向量库检索相关上下文
        ├── 构造系统提示词和用户问题
        ├── 调用 OpenAI-compatible LLM 网关
        └── 返回回答和引用
```

### 10.3 LLM 网关配置

后端支持：

```text
FOURZ_API_BASE
FOURZ_API_MODEL
FOURZ_API_KEY
DEEPSEEK_API_BASE
DEEPSEEK_API_MODEL
DEEPSEEK_API_KEY
```

优先级：

```text
FOURZ > DEEPSEEK > 规则兜底
```

### 10.4 后续增强方向

建议后续加入：

- RAG 引用来源展示
- 冲突条款 LLM 二次复核
- 根据人工追加提示词重新生成建议
- AI 生成审核意见草稿
- AI 生成标准对齐报告

---

## 11. 前端页面与接口对应关系

### 11.1 登录与用户

| 页面 | 路由 | 接口 |
|---|---|---|
| 登录 | `/login` | `POST /api/auth/login` |
| 忘记密码 | `/forgot-password` | `POST /api/auth/forgot-password` |
| 用户资料 | `/user/profile` | `GET/PUT /api/user/profile` |
| 修改密码 | `/user/change-password` | `POST /api/user/change-password` |
| 用户管理 | `/user/admin` | `GET/PUT /api/user/admin/users` |

### 11.2 标准治理

| 页面 | 路由 | 接口 |
|---|---|---|
| 工作台 | `/` | `GET /api/workbench/dashboard` |
| 标准数据库 | `/database` | `GET /api/standards` |
| 标准详情 | `/database/:standardId` | `GET /api/standards/{id}`, `GET /api/standards/{id}/content` |
| 标准下载 | 标准详情操作 | `GET /api/standards/{id}/download` |
| 智能检索 | `/search` | `GET /api/search` |
| 标准导入 | `/import` | `POST /api/import/upload`, `POST /api/import` |
| 导入历史 | `/import/history` | `GET /api/import/history` |

### 11.3 标准对齐

| 页面 | 路由 | 接口 |
|---|---|---|
| 创建对齐任务 | `/alignment` | `POST /api/alignment/tasks`, `POST /api/alignment/chat` |
| 对齐任务管理 | `/alignment/tasks` | `GET /api/alignment/tasks`, `DELETE /api/alignment/tasks/{id}`, `POST /api/alignment/tasks/{id}/retry` |
| 对齐结果 | `/alignment/result?taskId=xxx` | `GET /api/comparison/task/{id}`, `GET /api/comparison/stats/{id}`, `GET /api/comparison/conflicts/{id}` |
| 人工决策 | 对齐结果页 | `POST /api/alignment/tasks/{id}/manual-mapping` |
| 审核发布 | 任务管理页 | `POST /api/alignment/tasks/{id}/review` |
| 已发布结果 | 后续页面 | `GET /api/alignment/published` |

---

## 12. 全链路 Pipeline 汇总

### 12.1 登录认证 Pipeline

```text
用户输入账号密码
        │
        ▼
POST /api/auth/login
        │
        ├── 查询用户
        ├── 校验密码
        ├── 校验账号状态
        ├── 生成 JWT
        └── 返回 token + user
        │
        ▼
前端保存 token
        │
        ▼
后续请求自动携带 Authorization: Bearer token
```

### 12.2 标准治理 Pipeline

```text
管理员上传标准
        │
        ▼
文件解析与文本抽取
        │
        ▼
标准元数据入库
        │
        ▼
解析文本保存
        │
        ▼
向量索引构建
        │
        ▼
标准数据库可检索 / 可查看 / 可下载
        │
        ▼
工作台统计更新
```

### 12.3 标准检索 Pipeline

```text
用户输入关键词
        │
        ▼
GET /api/search
        │
        ├── 标准元数据检索
        ├── 文本关键词检索
        ├── 向量语义检索
        ├── 结果融合去重
        └── 生成命中摘要
        │
        ▼
前端展示标准列表和相关片段
```

### 12.4 标准对齐 Pipeline

```text
管理员选择标准 A、标准 B
        │
        ▼
创建对齐任务
        │
        ▼
后台执行对齐
        │
        ├── 加载标准正文
        ├── 条款切分
        ├── 语义匹配
        ├── 冲突识别
        ├── 优先级排序
        ├── 生成建议
        └── 记录未匹配条款
        │
        ▼
对齐结果入库到 result_json
        │
        ▼
前端查看结果
        │
        ▼
人工接受 / 拒绝 / 修改
        │
        ▼
提交审核
        │
        ▼
管理员审核通过并发布
        │
        ▼
普通用户查看已发布结果
```

### 12.5 AI 助手 Pipeline

```text
用户提问
        │
        ▼
是否选择两个标准？
        │
        ├── 是：加载两个标准正文
        └── 否：检索标准库和向量库上下文
        │
        ▼
构造 Prompt
        │
        ▼
调用 LLM 网关
        │
        ▼
返回回答 + 引用
```

### 12.6 审核发布 Pipeline

```text
对齐任务 completed
        │
        ▼
任务处于 draft
        │
        ▼
创建人或管理员提交审核 submit
        │
        ▼
任务处于 submitted
        │
        ├── 管理员 reject：任务 rejected
        └── 管理员 approve：任务 approved
                         │
                         ▼
                    管理员 publish
                         │
                         ▼
                    任务 published
                         │
                         ▼
                普通用户可查看发布结果
```

---

## 13. 数据模型概览

### 13.1 User

用于用户认证和权限控制：

- `username`
- `email`
- `password_hash`
- `role`
- `is_active`
- `avatar`

### 13.2 Standard

用于标准元数据管理：

- `standard_no`
- `name`
- `version`
- `status`
- `category`
- `department`
- `description`
- `source_file`

### 13.3 AlignmentTask

用于标准对齐任务和审核发布：

- `user_id`
- `input_text`
- `status`
- `options`
- `result_json`
- `completed_at`
- `review_status`
- `review_notes`
- `reviewed_by`
- `reviewed_at`
- `published_at`

### 13.4 ImportHistory

用于导入历史记录：

- 文件名
- 保存文件名
- 上传人
- 导入状态
- 成功数量
- 失败数量
- 错误信息
- 创建时间

---

## 14. 部署与运行设计

### 14.1 后端运行

```bash
cd semAlign_backend
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```

访问：

```text
http://localhost:8000
http://localhost:8000/health
http://localhost:8000/docs
```

### 14.2 前端运行

```bash
cd semAlign
npm install
npm run dev
```

访问：

```text
http://localhost:5173
```

### 14.3 环境配置

关键配置：

```text
DATABASE_URL=sqlite:///./data/semalign.db
SECRET_KEY=...
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
UPLOAD_DIR=./data/uploads
TEXT_OUTPUT_DIR=./data/texts
CHROMA_DB_DIR=./data/chroma_db
EMBEDDING_MODEL_DIR=./models/gte-multilingual-base
FOURZ_API_BASE=...
FOURZ_API_MODEL=...
FOURZ_API_KEY=...
```

生产环境建议：

- 使用 PostgreSQL 替代 SQLite。
- 使用对象存储或共享文件系统保存原始标准文档。
- 使用独立向量数据库服务。
- 使用 Redis + Celery/RQ 替代 FastAPI BackgroundTasks。
- 使用 HTTPS 和更严格的 CORS。
- 使用 Alembic 管理数据库迁移。

---

## 15. 当前状态

- 用户登录与 JWT 认证。
- 标准上传、解析、导入。
- 标准数据库列表、筛选、详情、下载。
- 工作台统计和动态。
- 智能检索。
- 标准对齐任务创建、后台执行、任务列表、删除、重试。
- 对齐结果查看、冲突分页。
- 人工接受、拒绝、手动修改。
- AI 标准对齐助手。
- 管理员用户管理、用户资料、修改密码。
- 对齐审核发布的基础字段和接口。

---

## 16. 总结

中航信标准治理系统以标准文档为核心资产，通过标准导入、标准数据库、智能检索、语义对齐、冲突识别、人工决策、审核发布和用户权限管理，形成标准治理闭环。

系统总体 Pipeline 可以概括为：

```text
标准文档上传
  → 文档解析与文本抽取
  → 标准元数据入库
  → 向量索引构建
  → 标准检索与查看
  → 标准语义对齐
  → 冲突识别与建议生成
  → 人工决策
  → 管理员审核发布
  → 普通用户查看已发布治理结果
```

