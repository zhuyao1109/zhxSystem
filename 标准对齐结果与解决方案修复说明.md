# 标准对齐结果与解决方案修复说明

## 修复时间
2026-04-28

## 修复的问题

### 1. ✅ 完善对齐任务列表页面功能

**问题描述：**
- 缺少重新对齐功能，当前只有删除功能

**修复方案：**
- 添加后端接口：`POST /api/alignment/tasks/{task_id}/retry`
- 添加前端重新对齐按钮，带加载动画
- 重新对齐会使用原任务的参数重新执行对齐

**修改文件：**
- 后端：`semAlign_backend/routers/alignment.py`
  - 新增 `retry_alignment_task()` 函数（行号：455-576）
- 前端：`semAlign/src/features/alignment/AlignmentTasks.tsx`
  - 添加 `RefreshCw` 图标导入
  - 添加 `retryingTaskId` 状态
  - 添加 `handleRetry()` 函数
  - 添加重新对齐按钮UI
- API：`semAlign/src/api/modules/alignment.ts`
  - 添加 `retryTask()` 方法
- Service：`semAlign/src/features/alignment/alignment.service.ts`
  - 添加 `retryTask()` 方法

**功能特性：**
- 点击重新对齐按钮时显示旋转动画
- 重新对齐完成后自动刷新任务列表
- 保留原任务的所有参数（标准组、优先级规则等）
- 会覆盖原有的对齐结果

---

### 2. ✅ 明确位置信息显示

**问题描述：**
- 位置信息 `A[正文#1] vs B[正文#1]` 只显示索引号，没有显示具体内容片段

**修复方案：**
- 在位置信息中显示内容的前30个字符
- 格式：`A[正文#1: "内容前30字..."] vs B[正文#2: "内容前30字..."]`

**修改文件：**
- 前端：`semAlign/src/features/alignment/AlignmentResult.tsx`
  - 行号：130-135

**修改前：**
```tsx
位置：A[{c.location?.standard1_section || '正文'}#{c.location?.standard1_clause_index ?? '-'}] 
vs B[{c.location?.standard2_section || '正文'}#{c.location?.standard2_clause_index ?? '-'}]
```

**修改后：**
```tsx
位置：A[{c.location?.standard1_section || '正文'}#{c.location?.standard1_clause_index ?? '-'}
{c.standard1?.content && `: "${c.standard1.content.slice(0, 30)}${c.standard1.content.length > 30 ? '...' : ''}"`}]
vs B[{c.location?.standard2_section || '正文'}#{c.location?.standard2_clause_index ?? '-'}
{c.standard2?.content && `: "${c.standard2.content.slice(0, 30)}${c.standard2.content.length > 30 ? '...' : ''}"`}]
```

**效果：**
- 用户可以直接看到冲突条款的内容片段
- 不需要滚动到下方才能看到具体内容
- 提高了信息的可读性和可用性

---

### 3. ✅ 实现手动对应操作功能

**问题描述：**
- 系统显示"推荐：规则建议接近，需要人工决策"，但没有手动对应操作的UI和接口
- 用户无法确认或修改对齐建议

**修复方案：**
- 添加后端接口保存用户的手动决策
- 添加前端UI让用户可以接受、拒绝或修改建议
- 决策保存在任务的 `result_json` 中

**后端实现：**

**文件：** `semAlign_backend/routers/alignment.py`

**新增数据模型：**
```python
class ManualMappingRequest(BaseModel):
    """手动映射请求"""
    conflict_id: str
    decision: str  # "accept" | "reject" | "modify"
    modified_recommendation: str | None = None
    notes: str | None = None

class ManualMappingResponse(BaseModel):
    """手动映射响应"""
    conflict_id: str
    decision: str
    updated_at: str
```

**新增接口：**
- 路由：`POST /api/alignment/tasks/{task_id}/manual-mapping`
- 函数：`save_manual_mapping()`
- 行号：454-560

**接口功能：**
1. 验证任务是否存在且属于当前用户
2. 在任务结果中查找对应的冲突
3. 保存用户的决策（accept/reject/modify）
4. 记录决策时间、用户信息、备注等
5. 返回更新结果

**前端实现：**

**文件：** `semAlign/src/features/alignment/AlignmentResult.tsx`

**新增接口定义：**
```typescript
interface ManualDecision {
  decision: string;
  username?: string;
  updated_at?: string;
  modified_recommendation?: string;
  notes?: string;
}

interface ComparisonConflictData {
  // ... 其他字段
  manual_decision?: ManualDecision;
}
```

**新增功能：**
1. `handleManualDecision()` 函数 - 处理用户决策
2. 三个操作按钮：
   - **接受建议**（绿色）- 同意系统建议
   - **拒绝建议**（红色）- 不同意系统建议
   - **手动修改**（黄色）- 输入自定义说明
3. 决策状态显示 - 显示已做的决策和时间

**UI效果：**
- 未决策时显示三个操作按钮
- 已决策时显示决策结果（接受/拒绝/修改）和时间
- 按钮有禁用状态，防止重复提交
- 使用不同颜色区分决策类型

**API调用：**

**文件：** `semAlign/src/api/modules/alignment.ts`

```typescript
saveManualMapping: async (
  taskId: string,
  data: {
    conflict_id: string;
    decision: 'accept' | 'reject' | 'modify';
    modified_recommendation?: string;
    notes?: string;
  }
): Promise<ApiResponse<{
  conflict_id: string;
  decision: string;
  updated_at: string;
}>>
```

---

## 数据流程

### 手动决策流程：

1. **用户操作**
   - 用户在对齐结果页面查看冲突
   - 点击"接受建议"、"拒绝建议"或"手动修改"按钮

2. **前端处理**
   - 调用 `handleManualDecision()` 函数
   - 设置 `savingDecision` 状态（显示加载状态）
   - 调用 `alignmentApi.saveManualMapping()` API

3. **后端处理**
   - 验证任务和用户权限
   - 在 `result_json.conflicts` 中查找对应冲突
   - 添加 `manual_decision` 字段保存决策
   - 更新数据库

4. **前端更新**
   - 更新本地 `conflicts` 状态
   - 隐藏操作按钮
   - 显示决策结果

---

## 测试建议

### 1. 重新对齐功能测试
- 创建一个对齐任务
- 在任务列表页面点击重新对齐按钮
- 验证按钮显示旋转动画
- 验证任务状态更新为 processing
- 验证对齐完成后状态更新为 completed
- 验证结果被正确更新

### 2. 位置信息显示测试
- 查看对齐结果页面
- 验证位置信息显示格式正确
- 验证内容片段显示（前30字符）
- 验证长内容显示省略号

### 3. 手动决策功能测试
- 查看对齐结果页面的冲突列表
- 点击"接受建议"按钮
  - 验证决策被保存
  - 验证按钮消失，显示决策结果
  - 验证决策时间正确显示
- 点击"拒绝建议"按钮
  - 验证决策类型为 reject
  - 验证显示红色标识
- 点击"手动修改"按钮
  - 验证弹出输入框
  - 输入修改说明
  - 验证决策和说明被保存
- 刷新页面
  - 验证决策状态被保留（从后端加载）

---

## 影响范围

**后端：**
- 文件：`semAlign_backend/routers/alignment.py`
- 新增接口：
  - `POST /api/alignment/tasks/{task_id}/retry` - 重新对齐
  - `POST /api/alignment/tasks/{task_id}/manual-mapping` - 保存手动决策

**前端：**
- 文件：
  - `semAlign/src/features/alignment/AlignmentTasks.tsx` - 任务列表页面
  - `semAlign/src/features/alignment/AlignmentResult.tsx` - 结果展示页面
  - `semAlign/src/api/modules/alignment.ts` - API 调用
  - `semAlign/src/features/alignment/alignment.service.ts` - 服务层

**数据库：**
- 无需修改表结构
- 决策数据保存在 `alignment_tasks.result_json` 字段中

---

## 技术亮点

1. **状态管理优化**
   - 使用 `savingDecision` 状态防止重复提交
   - 本地状态更新提供即时反馈

2. **用户体验优化**
   - 重新对齐按钮带旋转动画
   - 决策按钮有禁用状态
   - 不同决策类型使用不同颜色

3. **数据持久化**
   - 决策保存在任务结果中
   - 刷新页面后决策状态保留

4. **错误处理**
   - API 调用失败时显示错误提示
   - 验证任务和冲突是否存在

---

## 后续优化建议

1. **批量决策**
   - 支持一次性接受/拒绝多个建议

2. **决策历史**
   - 记录决策的修改历史
   - 支持撤销决策

3. **协作功能**
   - 多人协作决策
   - 决策审批流程

4. **导出功能**
   - 导出决策报告
   - 包含所有手动决策的汇总

5. **UI 优化**
   - 使用模态框代替 `prompt()` 和 `alert()`
   - 添加决策统计（已决策/待决策数量）
